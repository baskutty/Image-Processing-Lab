For equal dimension both convolution gives same output.

If dimensions are unequal the result will be different.If dimensions are both even or both odd the output matrix with the smaller dimension is a portion of the output matrix with the larger dimension.
